/*class MonkeyBrushError implements Error {
    public name = "MonkeyBrushError";

    constructor(public message: string) {
        if (typeof console !== "undefined") {
            console.log(`{$this.name} exception: ${msg}`);
        }
    }
};*/
