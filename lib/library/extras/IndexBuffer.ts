/*class IndexBuffer {
	protected _attribs = [];
	constructor(data, usage = gl.STATIC_DRAW) {

	};
	public attrib(name: string, size: number, type: enum, normalized: boolean = false) {

	}
	public draw(mode, count, offset) {
		const gl = Core.getInstance().getGL();
		gl.drawElements(mode, count, this._type, offset);
	}
};

export { IndexBuffer };*/