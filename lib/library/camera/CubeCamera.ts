// import { PerspectiveCamera } from "./PerspectiveCamera";
// import { Vect3 } from "../maths/Vect3";

class CubeCamera {
    constructor() {
        // const
        //     near = 0.0001,
        //     far = 1000.0;
        //
        // let camPX = new PerspectiveCamera(90.0, 1.0, near, far);
        // camPX.up = ( 0, - 1, 0 );
        // camPX.lookAt( new Vect3( 1, 0, 0 ) );
        //
        // let camNX = new PerspectiveCamera(90.0, 1.0, near, far);
        // camNX.up = ( 0, - 1, 0 );
        // camNX.lookAt( new Vect3( - 1, 0, 0 ) );
        //
        // let camPY = new PerspectiveCamera(90.0, 1.0, near, far);
        // camPY.up = ( 0, 0, 1 );
        // camPY.lookAt( new Vect3( 0, 1, 0 ) );
        //
        // let camNY = new PerspectiveCamera(90.0, 1.0, near, far);
        // camNY.up = ( 0, 0, - 1 );
        // camNY.lookAt( new Vect3( 0, - 1, 0 ) );
        //
        // let camPZ = new PerspectiveCamera(90.0, 1.0, near, far);
        // camPZ.up = ( 0, - 1, 0 );
        // camPZ.lookAt( new Vect3( 0, 0, 1 ) );
        //
        // let camNZ = new PerspectiveCamera(90.0, 1.0, near, far);
        // camNZ.up = ( 0, - 1, 0 );
        // camNZ.lookAt( new Vect3( 0, 0, - 1 ) );
    }
}
