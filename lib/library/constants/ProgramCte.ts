
"use strict";

namespace ProgramCte {
    export enum mode {
        read_file,
        read_script,
        read_text
    };
    export enum shader_type {
        vertex,
        fragment
    }
};

export default ProgramCte;